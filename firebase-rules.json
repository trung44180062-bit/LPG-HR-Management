{
  "_huong_dan": [
    "Firebase Console -> Realtime Database -> Rules -> dán toàn bộ nội dung dưới đây -> Publish.",
    "Đổi 'shiftwork_v2' nếu APP_CFG.dbPath trong js/config.js khác.",
    "Bộ luật này ứng với bản hiện tại: app dùng đăng nhập ẩn danh (anonymous) và tự",
    "kiểm tra mật khẩu ở phía trình duyệt. Nó CHẶN người ngoài (chưa đăng nhập ẩn danh)",
    "và chặn ghi dữ liệu rác, nhưng CHƯA phân biệt được nhân viên với quản trị —",
    "muốn tới mức đó phải chuyển sang Firebase Authentication, xem mục 4 của BAO-MAT.md.",
    "",
    "Lưu ý quan trọng: KHÔNG dùng '.read': true. Để mở là ai có link cũng tải hết dữ liệu."
  ],

  "rules": {
    ".read": false,
    ".write": false,

    "shiftwork_v2": {
      ".read": "auth != null",
      ".write": "auth != null",

      "employees":  { ".validate": "newData.hasChildren() || newData.val() == null" },
      "base":       { ".validate": true },
      "over":       { ".validate": true },
      "requests":   { ".validate": true },
      "settings":   { ".validate": true },
      "printLog":   { ".validate": true },
      "meta":       { ".validate": true },
      "rev":        { ".validate": "newData.isNumber()" },

      "accounts": {
        ".read": "auth != null",
        ".write": "auth != null",
        "$empId": {
          "hash":  { ".validate": "newData.isString() && newData.val().length <= 128" },
          "salt":  { ".validate": "newData.isString() && newData.val().length <= 64" },
          "alg":   { ".validate": "newData.isString() && (newData.val() == 'pbkdf2' || newData.val() == 'sha256')" },
          "it":    { ".validate": "newData.isNumber()" },
          "init":  { ".validate": "newData.isBoolean()" },
          "at":    { ".validate": "newData.isNumber()" },
          "by":    { ".validate": "newData.isString()" },
          "$other": { ".validate": false }
        }
      }
    }
  }
}
